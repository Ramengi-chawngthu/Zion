import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#FFF",
      pink: "#A06294",
    },
    secondary: {
      main: "#742FDA",
      viridian: "#03503E",
    },
    ternary: {
      main: "#000",
      secondary: "#EBEBFF",
      apple: "#F5F5F7",
    },
  },
});

export default theme;
